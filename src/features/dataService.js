//Set Room
const setRoom = async (arg) => {
    return arg
}

const setViewer = async (arg) => {
    return arg
}

const setItem = async (arg) => {
    return arg
}

const dataService = {
    setRoom,
    setViewer,
    setItem
}

export default dataService